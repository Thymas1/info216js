function link() {
window.onload = function() {
        // prepare the link
        item_link = document.createElement("A");
        item_link.href = "vislad.html?id=" + poi.ID;
        link_text = document.createTextNode(Addresse["poi.AddressInfo.AddressLine1"]);

    }
}
